<nav class="flex items-center justify-center w-full p-2 space-x-4 text-white border-t border-b shadow-2xl backdrop-blur-sm border-white/20 bg-white/15">
	<a href="{{ app.request.headers.get('referer')|default('/') }}" class="btn btn-sm btn-circle btn-error">
		<i class="fa-sharp fa-solid fa-arrow-left fa-lg"></i>
	</a>
	<span class="text-2xl">{{ page_title }}</span>
</nav>
